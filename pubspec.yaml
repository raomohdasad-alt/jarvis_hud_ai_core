name: jarvis_final_ai
description: JARVIS FINAL SYSTEM
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0
  shared_preferences: ^2.2.3
  speech_to_text: ^6.6.0
  flutter_tts: ^4.0.2

flutter:
  uses-material-design: true
